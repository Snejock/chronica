[project]
name = "tg-sender"
version = "0.1.0"
description = "Sends subscribed story news to Telegram via the Bot API"
requires-python = ">=3.14"
dependencies = [
    "chronica-common",
    "confluent-kafka[avro,schema-registry]>=2.13.0",
    "httpx[http2]>=0.28.1",
]

[tool.uv.sources]
chronica-common = { workspace = true }
