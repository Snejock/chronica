server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA-фоллбэк: неизвестные пути отдаём как index.html, чтобы клиентский роутинг
    # (когда появится) сам разбирался с адресом.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
