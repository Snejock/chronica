input:
  generate:
    interval: "20m"
    mapping: 'root = {}'

pipeline:
  processors:
    - label: postgres
      sql_raw:
        driver: postgres
        dsn: postgres://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DATABASE}?sslmode=disable
        query: |
          REFRESH MATERIALIZED VIEW bds.b_news_unified;

    - log:
        level: INFO
        message: '[✅ REFRESHED] bds.b_news_unified'

output:
  drop: {}
